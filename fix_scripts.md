# 系统修复脚本说明

本文档详细介绍了TikTok账号管理系统的各种修复脚本及其用途，帮助管理员快速解决系统中可能出现的问题。

## 目录

1. [按钮显示问题修复](#按钮显示问题修复)
2. [服务器部署修复](#服务器部署修复)
3. [紧急修复脚本](#紧急修复脚本)
4. [完全重置脚本](#完全重置脚本)

## 按钮显示问题修复

### direct_fix.sh

此脚本用于修复账号管理页面中操作按钮不显示的问题。

**功能**:
- 备份当前文件到安全位置
- 修复CSS样式问题，确保按钮正确显示
- 检查HTML结构，确保操作按钮包含在正确的容器中
- 删除可能导致冲突的重复CSS规则

**使用方法**:
```bash
chmod +x direct_fix.sh
./direct_fix.sh
```

**注意事项**:
- 脚本执行后需要刷新浏览器缓存（Ctrl+F5）
- 如果问题仍然存在，请检查浏览器控制台是否有JavaScript错误

### fix_server_buttons.sh

此脚本专为服务器环境设计，用于修复生产环境中的按钮显示问题。

**功能**:
- 备份服务器上的当前文件
- 修复CSS样式问题
- 检查并修复HTML结构
- 修复Nginx配置（如果需要）
- 重启应用服务

**使用方法**:
```bash
# 在服务器上执行
sudo ./fix_server_buttons.sh
```

**注意事项**:
- 需要root权限执行
- 会自动重启应用和Nginx服务
- 执行后需要清除浏览器缓存

## 服务器部署修复

### fix_server.sh

此脚本用于修复服务器部署中的常见问题。

**功能**:
- 修复端口配置
- 更新Nginx配置
- 检查并修复权限问题
- 重启相关服务

**使用方法**:
```bash
sudo ./fix_server.sh
```

## 紧急修复脚本

### emergency_fix.sh

此脚本用于紧急情况下快速恢复系统功能。

**功能**:
- 回滚到最后一个已知正常的配置
- 重置数据库连接
- 重启所有服务

**使用方法**:
```bash
sudo ./emergency_fix.sh
```

## 完全重置脚本

### complete_reset.sh

此脚本用于完全重置系统到初始状态。

**功能**:
- 备份当前数据
- 重置数据库
- 重置所有配置文件
- 重新安装依赖

**使用方法**:
```bash
sudo ./complete_reset.sh
```

**警告**:
- 此脚本会删除所有现有数据，请确保已备份重要信息

## 故障排除

如果修复脚本无法解决问题，请参考以下步骤：

1. 检查日志文件（`/var/log/tiktok_account_system.log`）
2. 确认服务器资源使用情况（内存、CPU、磁盘空间）
3. 验证数据库连接是否正常
4. 检查网络连接和防火墙设置

如需更多帮助，请联系系统管理员或参考[故障排除文档](troubleshooting.md)。
